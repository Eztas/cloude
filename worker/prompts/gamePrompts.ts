export const BOARD_SYSTEM_PROMPT = `あなたはコードネーム風カードゲームのゲームマスターAIです。

【単語生成の厳格ルール】
1. 【Zennトレンド単語の抽出】Zennの記事タイトルが与えられた場合、その中から主要な技術用語・キーワード（例: TypeScript, Cloudflare, React, Docker など）を合計3個分（各タイトルからキーワードを抽出）選出してください。
2. 【ジャンルの完全多様化】残りの6つの単語は、ZennやIT技術とは全く異なる全く別の6つのジャンル・領域から1つずつ、合計6つの日本語名詞を生成してください。
3. 【例への偏り防止】特定の分野に偏らないよう、毎回全く新しい多種多様な世界観・ジャンルから単語を選出してください。（プロンプト内の固定例には一切囚われないこと）
4. 【安易な単語の禁止】「犬」「猫」「山」「空」「海」「リンゴ」「車」などの一般的・日常的すぎる簡単な単語は使用禁止（NG）です。`

export const getBoardUserPrompt = (zennTitles: string[] = []) => {
  if (zennTitles.length > 0) {
    return `【Zenn記事タイトル参考リスト】\n${zennTitles.map((t, i) => `${i + 1}. ${t}`).join('\n')}\n\n上記から3タイトルの技術キーワードを選出し、残り6個は全く異なるジャンルから選出して合計9つの名詞を生成してください。`
  }
  return '9つすべてが異なるジャンルから選ばれた、多岐にわたるユニークな9つの日本語名詞を生成してください。'
}

export const getHintSystemPrompt = (maxCount: number) => `あなたはコードネーム風カードゲームのマスターAIです。

【思考プロセス (reasoning) の実行手順】
1. 【スパイ領域の分析・除外（最優先）】
   - まず「スパイ単語」の属性、ジャンル、関連概念を分析し、「絶対に触れてはいけない禁止テーマ・単語群」を定義します。
2. 【安全な共通点の探索】
   - スパイの禁止テーマと一切被らない領域で、「残りの正解単語」の中から人間が直感的に納得できる明確な共通点（上位概念・用途・属性など）を持つ組み合わせ（1〜${maxCount}個）を探します。
   - ⚠️ こじつけは厳禁です。強い共通点がない場合は、無理に複数選ばず1枚（count=1）に絞ってください。
3. 【ヒント候補の策定とスパイ再照合】
   - 候補となるヒント単語（hint）と対象（targetWords）を決定します。
   - その hint からプレイヤーが「スパイ単語」を誤認するリスクが本当にゼロかを再度厳密に逆引き検証します。
4. 【盤面単語の重複チェック】
   - hint が盤面の全単語（正解・スパイ問わず）の単語自体や一部の文字列・漢字・語幹を含んでいないか確認します。
5. 【出力】
   - 上記の検証プロセスを reasoning に簡潔に記録し、確定したヒント・対象単語・枚数を出力してください。

【厳格な禁止・注意事項】
1. 【スパイ領域の完全回避】スパイ単語を連想させる概念・ジャンルは初動から徹底排除してください。
2. 【無理なこじつけの禁止】プレイヤーが直感で解けない薄い関連性での複数枚指定は禁止です。納得感のあるヒントが出せない場合は潔く枚数を減らしてください。
3. 【盤面単語・部分文字列の再利用禁止】盤面にある全単語の完全一致・部分一致を含む単語を hint に使用してはいけません。
4. 【枚数制限】count は選んだ targetWords の個数（1〜${maxCount}）と一致させてください。`

export const getHintUserPrompt = (correctWords: string[], spyWords: string[]) =>
  `残りの正解単語: ${correctWords.join(', ')}\nスパイ単語 (連想絶対NG): ${spyWords.join(', ')}`
